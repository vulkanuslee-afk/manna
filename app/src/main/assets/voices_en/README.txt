이 폴더에 generate_voice.py로 생성한 mp3 파일들과 manifest.json을 넣으세요.

넣는 방법:
1) PC에서 generate_voice.py --lang en 실행 -> voices_en 폴더 생성됨
2) voices_en 폴더 안의 모든 파일(mp3 + manifest.json)을 이 폴더에 복사
3) Android Studio에서 빌드

파일이 없어도 앱은 정상 작동합니다 (기기 내장 음성으로 자동 대체).
